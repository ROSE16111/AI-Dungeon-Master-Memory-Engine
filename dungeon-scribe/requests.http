@baseUrl = http://localhost:3000
@campaign = cmp1
@ctype = application/json

### Inventory — alias “Thal”
POST {{baseUrl}}/api/chat-turn
Content-Type: {{ctype}}

{
    "campaignId": "{{campaign}}",
    "text": "What items does Thal have?"
}

### Stats — Elaria’s INT
POST {{baseUrl}}/api/chat-turn
Content-Type: {{ctype}}

{
    "campaignId": "{{campaign}}",
    "text": "What is Elaria's intelligence?"
}

### Location events — Vine-Covered Ruins
POST {{baseUrl}}/api/chat-turn
Content-Type: {{ctype}}

{
    "campaignId": "{{campaign}}",
    "text": "What did the party do in Vine-Covered Ruins?"
}

### Session summary — session 1
POST {{baseUrl}}/api/chat-turn
Content-Type: {{ctype}}

{
    "campaignId": "{{campaign}}",
    "text": "Summarize session 1"
}

### Session summary — last session
POST {{baseUrl}}/api/chat-turn
Content-Type: {{ctype}}

{
    "campaignId": "{{campaign}}",
    "text": "Recap last session"
}

### (Optional) Needs clarification example — unknown name
POST {{baseUrl}}/api/chat-turn
Content-Type: {{ctype}}

{
    "campaignId": "{{campaign}}",
    "text": "What items does Boblin have?"
}
